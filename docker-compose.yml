services:
  lumos-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    depends_on:
      - chroma-db

  chroma-db:
    image: chromadb/chroma
    volumes:
      - ./lumos_db:/chroma/data
    ports:
      - "8000:8000"