export const AGENT_NAME = "LUMOS";
const WAKE_WORD = "Lumos";
const SHUTDOWN_WORD = "System shut down";

export const getWakeWord = WAKE_WORD.trim().toLowerCase();

export const getShutdownWord = SHUTDOWN_WORD.trim().toLowerCase()
